import { CompetitionSingInData } from './competition-sing-in-data';

describe('CompetitionSingInData', () => {
  it('should create an instance', () => {
    expect(new CompetitionSingInData()).toBeTruthy();
  });
});
